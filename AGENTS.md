# BaioHack — Agent Notes

- Preserve existing marketing copy and business information unless explicitly asked to change it.
- Keep the branch in a working state: run `bun run build` before finishing substantive changes.
- Avoid rewriting published git history (force push, rebase/amend/squash on pushed commits).
